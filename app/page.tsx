export default function HomePage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col justify-center gap-4 p-8">
      <h1 className="text-2xl font-semibold tracking-tight">Studio Class Booking</h1>
      <p className="text-slate-600 dark:text-slate-400">
        Schema and authentication are in; the booking lifecycle and the real interface land in the
        phases that follow — see <code className="font-mono text-sm">docs/plan.md</code>.
      </p>
    </main>
  )
}
